{
  "name": "replayhouse-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/**/*.test.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "chdb-wasm": "^0.3.0"
  }
}
